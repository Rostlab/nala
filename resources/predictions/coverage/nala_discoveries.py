
# tmVar 3366cc
A = set([

])

# SETH ffcc00
B = set([

])

# nala 669933
C = set([

])

# ------------------------------------------------

A_SIZE = len(A)
B_SIZE = len(B)
C_SIZE = len(C)

AiB_SIZE = len(A.intersection(B))
AiC_SIZE = len(A.intersection(B))
BiC_SIZE = len(B.intersection(C))

AiBiC_SIZE = len(A.intersection(B).intersection(C))
